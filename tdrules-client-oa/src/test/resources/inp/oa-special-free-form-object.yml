openapi: "3.0.0"
info:
  version: 1.0.0
  title: >
    Special objects that must not be handled like a regular object: 
      Free-form object: is converted into a primitive type
components:
  schemas:
    TestFreeForm:
      properties:
        id:
          type: integer
        freeForm:
          type: object

    #Other allowed forms, See https://swagger.io/docs/specification/v3_0/data-models/dictionaries/
    TestFreeForm2:
      properties:
        freeForm:
          type: object
          additionalProperties: true
    TestFreeForm3:
      properties:
        freeForm:
          type: object
          additionalProperties: {}

    #Without object type, ignored
    #TODO true or {} should generate a free form, not empty or array, to correct in a future issue
    #TODO check also if free form attributes should be nullable
    TestFreeFormA1:
      properties:
        id:
          type:true
      additionalProperties: true
    TestFreeFormA2:
      properties:
        id:
          type:true
      additionalProperties: false
    TestFreeFormA3:
      properties:
        id:
          type:true
      additionalProperties: {}
        